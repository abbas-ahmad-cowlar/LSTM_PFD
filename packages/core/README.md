# Core ML Package

This package will contain the core ML modules after full migration:

- `models/` - Model architectures (CNN, ResNet, PINN, Transformer, etc.)
- `training/` - Training utilities, schedulers, callbacks
- `evaluation/` - Evaluators and metrics
- `features/` - Feature extraction and engineering
- `pipelines/` - ML pipelines
