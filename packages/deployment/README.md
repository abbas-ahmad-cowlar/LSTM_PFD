# Deployment Package

This package will contain deployment utilities after full migration:

- `api/` - FastAPI server
- `quantization/` - Model optimization
- `docker/` - Containerization configs
