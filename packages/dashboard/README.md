# Dashboard Package

This package will contain the Plotly Dash dashboard after full migration:

- `layouts/` - Page layouts
- `callbacks/` - Interactive callbacks
- `services/` - Business logic services
- `models/` - Database models
