# Testing requirements for LSTM_PFD
# Install with: pip install -r requirements-test.txt
# Minimum versions matching the verified Python 3.14 environment
# (see requirements.lock.txt for the exact frozen set).

# Testing framework
pytest>=9.0
pytest-cov>=7.0
pytest-mock>=3.12

# API tests
fastapi>=0.115
httpx>=0.27

# ONNX export/inference tests
onnx>=1.20
onnxruntime>=1.26

# Code quality
black>=24.0
isort>=5.13
flake8>=7.0
