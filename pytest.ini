[pytest]
# Pytest configuration for LSTM_PFD

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
# Dashboard tests are excluded by default while the dashboard is frozen
# (Convergence Plan Phase D). Run them explicitly with: pytest -m dashboard
addopts =
    --verbose
    --strict-markers
    --tb=short
    --disable-warnings
    -ra
    -m "not dashboard"
    --cov=.
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-config=.coveragerc

# Markers
markers =
    unit: Unit tests for individual components
    integration: Integration tests for pipelines
    benchmark: Performance benchmarks
    slow: Slow tests (>1 second)
    gpu: Tests requiring GPU
    dashboard: Dashboard tests (frozen until Convergence Phase D)

# Coverage options
[coverage:run]
source = .
omit =
    */tests/*
    */venv/*
    */env/*
    setup.py
    conftest.py

[coverage:report]
precision = 2
show_missing = True
skip_covered = False

[coverage:html]
directory = htmlcov
