% T3 -- the other four regimes at a glance (each negative, record-level). Sources:
% results/band_energy_reruns/summary_record_level.json (8.2 data-eff, 8.3 OOD);
% results/interpretability/alignment.json (8.6a); results/calibration/calibration.json (8.6b)
\begin{table}[t]
  \centering
  \caption{The other four stress-test regimes, each record-level and each
    negative. ``Verdict'' is against each regime's pre-registered or stated
    criterion; none yields a statistically supported, seed-robust advantage for
    the tested method.}
  \label{tab:regimes}
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.19\linewidth}%
                  >{\raggedright\arraybackslash}p{0.52\linewidth}%
                  >{\raggedright\arraybackslash}p{0.20\linewidth}}
    \toprule
    Regime & Record-level result & Verdict \\
    \midrule
    Data-efficiency (protocol~\S8.2) &
      PC-CNN ahead of \texttt{resnet18} with non-overlapping $\pm1$\,std at 1 of
      3 reduced fractions ($\ge$2 required); tied at 10\%, 50\% &
      negative (neutral) \\
    \addlinespace
    Severity-OOD (protocol~\S8.3) &
      dir.\ A ceiling tie ($100$/$100$); dir.\ B $82.58$ vs $76.01$ but McNemar
      $p=0.39$, gap CI $[-2.27,+8.33]$ spans 0 &
      negative (direction-only) \\
    \addlinespace
    Interpretability (protocol~\S8.6a) &
      band-aware in-band IG fraction reverses: vanilla $0.146 >$ physics
      $0.099$; $\approx$0 for both on \texttt{lubrification} &
      negative (reverses) \\
    \addlinespace
    Calibration (protocol~\S8.6b) &
      both ECE $<0.03$; 5\,dB ECE direction flips between MC-dropout runs &
      negative (wash) \\
    \bottomrule
  \end{tabular}
\end{table}
